# Image provenance

- `../../public/rai-social.png` is a concept illustration, not runtime evidence.
- `workbench.jpg` and `agent-run.jpg` are actual local RAI captures from September 3, 2026. They show the retained WebMCP can attempt: 18 tool events, 3:08.2 elapsed. The workbench shows the released can; the timeline capture is a mid-attempt frame.
- Camera rendering includes the display-parity fix and is reconstructed from scene state, not retained raw pixels.
- These are local screenshots, not proof of a complete trial on the public host. Hosting checks are separate in `NATIVE_WEBMCP_EVIDENCE.md`.

There is no mascot in the app or these assets.
